#pragma once
// Generated by CMake from version.hpp.in — do not edit. The version is
// single-sourced in the root CMakeLists.txt project() call; DEVMGR_PRERELEASE
// supplies the prerelease suffix (release-versioning spec).
#include <string>
#include <string_view>

namespace devmgr::core {
inline constexpr int kVersionMajor = @PROJECT_VERSION_MAJOR@;
inline constexpr int kVersionMinor = @PROJECT_VERSION_MINOR@;
inline constexpr int kVersionPatch = @PROJECT_VERSION_PATCH@;
inline constexpr std::string_view kPrerelease = "@DEVMGR_PRERELEASE@";
inline constexpr std::string_view kVersion = "@DEVMGR_FULL_VERSION@";  // <semver>[-<prerelease>]

inline constexpr std::string_view version() {
    return kVersion;
}

// "<binary> <semver>[-<prerelease>]" — the exact --version stdout line every
// binary prints (release-versioning spec).
inline std::string versionLine(std::string_view binary) {
    std::string line(binary);
    line += ' ';
    line += kVersion;
    return line;
}
}  // namespace devmgr::core
