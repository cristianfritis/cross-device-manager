# Tasks: tui-instrument-panel-polish

Sequencing follows design.md Migration Plan: each numbered group leaves the build green and the app working. Verify each step with the container unit gate (rebuild the `unit` image first — no volume mount) before marking done. Coloring path is Option C (design.md Decision 1a): VMs gain read-only per-row state accessors, TUI colors from them, GUI unchanged under a DESIGN §9 parity exception.

## 1. Theme foundation (no UI change)

- [x] 1.1 Create `tui/src/theme.hpp`/`theme.cpp`: semantic role enum (accent/success/warning/danger/info/muted), `Theme` struct exposing FTXUI decorators, capability mode enum (full/mono/plain) resolved from `NO_COLOR`, `--no-color`/`--ascii`, `TERM=dumb`; decorators are identity in mono/plain
- [x] 1.2 Plumb `--no-color`/`--ascii`/`--unicode` flags through the TUI entry point into theme resolution (theme reaches `runTuiApp`; consumed by views in group 2)
- [x] 1.3 Create `tui/src/render_util.hpp`: status glyph helper (ASCII default `+ - ? !`, Unicode `● ○ ◉` opt-in, ASCII forced in mono/plain), kv row helper, region frame helper (ASCII border fallback in plain)
- [x] 1.4 Create `tui/tests/` with `test_theme.cpp` (8 tests: role→color mapping, capability downgrade, flag/env resolution, glyph policy, plain-ASCII frame); new `devmgr_tui_render` lib + `devmgr_tui_tests` exe wired into CMake + ctest
- [x] 1.5 Extend gates to cover `tui/`: format gate covers `tui/` (container clang-format-18: 214 files clean). clang-tidy gate line extended to `tui/src/*.cpp tui/src/views/*.cpp` in `.github/workflows/ci.yml`; container clang-tidy-18 exit 0 over all 10 tui files. `runTuiApp` size/cognitive-complexity + the nested event-loop lambda suppressed with `NOLINTBEGIN/END` (GUI `main_window.cpp` precedent — event-loop composition, render logic already extracted to `views/`); `marqueeWindow` swappable-params `NOLINTNEXTLINE`; the 150 ms tick literal named.

## 2. Per-view extraction (pure render functions, behavior-preserving, no color yet)

- [x] 2.1 Extract `tui/src/views/tab_bar.cpp` and `tui/src/views/status_bar.cpp` as pure `render(...)` functions + render tests at 120x32/100x28/80x24 (`test_bars_render.cpp`, 3 tests); shell delegates via `views::renderTabBar`/`renderStatusBar`, behaviour-preserving, `theme` now consumed (no more `[[maybe_unused]]`)
- [x] 2.2 Extract `tui/src/views/devices_view.cpp` + `test_devices_view_render.cpp` (three sizes, no row exceeds width, no out-of-bounds writes, selection/focus markers)
- [x] 2.3 Extract `tui/src/views/modules_view.cpp` + render tests (same assertions)
- [x] 2.4 Extract `tui/src/views/updates_view.cpp` + render tests (same assertions)
- [x] 2.5 Extract `tui/src/views/snapshots_view.cpp` + render tests (same assertions)
- [x] 2.6 Reduce `tui_app.cpp` to a thin shell composing the view functions; confirm no behavior change (manual smoke + full ctest)

## 3. VM per-row state accessors (app/; no wording/behavior change)

- [x] 3.1 `DeviceListVM::statusForRow(int) -> std::optional<core::DeviceStatus>` (nullopt for header/placeholder/oob) + unit tests over grouped/filtered/empty layouts
- [x] 3.2 `ModulesVM::signedForRow(int)` (signed/unsigned/undetermined; blacklisted if available) returning an optional state enum + unit tests
- [x] 3.3 `UpdatesVM::stateForRow(int)` (available/up-to-date/error, optional) + unit tests
- [x] 3.4 `SnapshotsVM::healthForRow(int) -> std::optional<core::SnapshotHealth>` plus HEAD / last-good row-marker predicates + unit tests
- [x] 3.5 `StatusLineVM::severity()` (ok/success/warning/danger/info tracked when the message is set) + unit tests
- [x] 3.6 Run `devmgr_tests` (container unit gate) green with the new accessor tests

## 4. Semantic coloring and layout polish (TUI maps accessor state → theme role)

- [x] 4.1 Devices: Active→success, Disabled→danger, Transitioning→warning, Error→danger, Unknown→muted; guard refusal→danger on status line; glyph+text paired everywhere
- [x] 4.2 Modules: signed→success, unsigned→danger, undetermined→muted, blacklisted→warning; security banner info in steady state, warning only when it explains a likely refusal
- [x] 4.3 Updates: available→info, up-to-date→muted success, error→danger
- [x] 4.4 Snapshots: healthy→default, corrupt→danger, unsupported→warning; HEAD + last-good markers accent
- [x] 4.5 Status line: success/warning/danger/info by `severity()`
- [x] 4.6 Border discipline: borders on major regions only (list/detail/status); separators + muted group headers for sub-regions; one legend line, one status line; verify 80–109 col switching layout intact

## 5. States matrix and color-independence proof

- [x] 5.1 Per-view states-matrix render tests: empty, loading, prompt, confirmation, refusal, failure
- [x] 5.2 Mono-mode assertions per view: state glyph AND state text present for every represented state (color-independence proof); plain-mode ASCII border check
- [x] 5.3 Update coloring tests to assert semantic role decorators in full mode

## 6. Exit gate

- [x] 6.1 Full local CI mirror (all green 2026-07-22): `check-format.sh --container` (clang-format-18, 214 clean); Qt + sdbus purity guards PASS; container unit gate rebuilt + `ctest` 533/533; container clang-tidy-18 over the full gate set incl `tui/src` + `tui/src/views` exit 0
- [x] 6.2 Document the DESIGN §9 temporary GUI parity exception (GUI color deferred; accessors GUI-ready) where the project records parity exceptions — added as a durable requirement in `specs/tui-presentation/spec.md` (syncs to the main spec on archive; honors the "no DESIGN.md edits" non-goal); `openspec validate --strict` green
- [ ] 6.3 Manual matrix (DESIGN §12.2 TUI rows): 120x32 / 100x28 / 80x24, `NO_COLOR`, `TERM=dumb`, keyboard-only Devices+Modules workflow; record results — *folded into pass-2 exit gate 12.3 (re-run in a real terminal)*
- [ ] 6.4 `openspec validate` change + specs green; user commits — *folded into pass-2 exit gate 12.4*

## 7. Pass 2 — core + VM data (no UI change)

- [x] 7.1 (T1) Add `core::displayDeviceName(const core::Device&)` + `core::displayDeviceIdentity(...)` (`core/{include/devmgr/core,src}/device_presentation.{hpp,cpp}`) as PURE FORMATTERS over EXISTING DTO fields — no libudev/libpci call, no bundled pci.ids, no sqlite, no new dependency, no daemon/IPC/ApiVersion change. **Step 0 (revised 2026-07-23, supersedes the earlier audit):** `scripts/probe-canonical-names.sh` (now with a `--resolve` tier simulation) measured 51 pci/usb devices on the dev box — `ID_MODEL_FROM_DATABASE` covers 43, `ID_VENDOR_FROM_DATABASE` covers 51/51, `ID_PCI_SUBCLASS_FROM_DATABASE` 38/39 PCI; 5 rows render as a bare kernel address today. The earlier audit's claim that the DTO carries no vendor/class strings was WRONG: `udev_device_mapper.cpp:98-102` copies the device's ENTIRE udev property map into `core::Device::properties`, and TUI/GUI construct `UdevDeviceEnumerator` in-process (`tui_app.cpp:209`, `gui_app.cpp:64`) on both the enumerate and hotplug paths — so the hwdb strings are already DTO data on every real path (test fakes leave `properties` empty and degrade to the raw-id tiers). The DTO field is `sysfsPath`, not `lastSysfsPath`. **Formatter contract (as built):** tier 1 `Device::name` when non-positional (+ direct `ID_MODEL_FROM_DATABASE` backstop) → tier 2 cleaned `ID_MODEL` (underscores→spaces; rejected when it is just the hex product id) → tier 3 `<shortVendor> <ID_PCI_SUBCLASS_FROM_DATABASE>` → tier 4 `<shortVendor> <ID_PCI_CLASS_FROM_DATABASE>` → tier 5 `<shortVendor> device` → tier 6 `<BUS> device`, then `(unknown device)`. Positional detection = `name == basename(sysfsPath)` (device_key.cpp:29-33 definition) OR PCI-BDF shape OR USB port-chain/root-hub shape. `shortVendor` prefers a bracketed pci.ids alias (`... [AMD]` → `AMD`) else strips one legal-form suffix (`Co., Ltd`, `Inc.`, `Corporation`, …); the vendor joins in tiers 3-5 ONLY — OWNER decision 2026-07-23: catalogued names keep the catalogue's wording (no churn on 43/51 rows, widest label 70 not 79 cols). `displayDeviceIdentity` = `vendorId:productId · <position>`, which is what distinguishes the 23 rows that share a name. **Wired:** `DeviceListVM::appendRows` (rows AND sort order), the filter haystack (canonical + raw name), and `DeviceDetailVM` `Name:` — list and detail read the same formatter, so R1 parity is structural. **Verified:** 20 new unit tests in `tests/unit/test_device_presentation.cpp` (tiers, positional shapes, vendor shortening table, junk-model rejection, empty/degenerate devices, identity disambiguation); full ctest 552/552 green; host format gate clean (container parity pending — docker daemon down, folded into 12.2).
- [x] 7.1b Vendor+class enrichment for uncatalogued rows — **folded into 7.1 and shipped there.** The owner-gate premise was false: no additive DTO field, no `UdevDeviceMapper` change and no ApiVersion decision are needed, because `Device::properties` already carries `ID_VENDOR_FROM_DATABASE` / `ID_PCI_{,SUB}CLASS_FROM_DATABASE` on every real path (see 7.1 Step 0). Measured effect on the dev box: rows rendering as a bare kernel address 5 → 0 (e.g. `0000:c5:00.4` → `AMD USB controller`, `3-3` → `Synaptics device`). Re-run `scripts/probe-canonical-names.sh --resolve` on other hardware to confirm the tier coverage generalises.
- [x] 7.2 (T2) Core criticality classifier (`core/{include/devmgr/core,src}/criticality.{hpp,cpp}`): `enum class Criticality {Ordinary, Important, Essential}`, `displayCriticality()` (the word that pairs with the marker in mono/plain), `classifyDevice(facts, sysfsPath)`, `classifyModule(name, refCount, holders)`. **OWNER sign-off 2026-07-23 — Decision 8 AMENDED from a name-list-only policy to facts-first + curated fallback.** Rationale found at apply time: `services::evaluateDisable`/`evaluateModuleUnload` already derive criticality from live system facts (root/boot backing paths, sole keyboard/pointer, refcount, holders) and are what the daemon actually enforces, so a curated-string marker could contradict the refusal the user hits — and Decision 8 itself calls misclassification safety-relevant. Devices therefore classify through the guard's own pure policy (Essential ⇔ the guard would refuse); modules use holders/refcount plus the Decision 8 curated essential list, which covers the case a refcount cannot see (amdgpu at refcount 0 still blanks the session). Curated list adopted as specified, exact-match only (no prefix/substring — it would over-warn); security modules (`apparmor`/`selinux`/`ima`/`integrity`/`lockdown`) map to Important, never Essential. + 15 unit tests in `tests/unit/test_criticality.cpp`, including one that asserts the marker and `evaluateDisable` agree rather than asserting a hardcoded expectation.
- [x] 7.3 (T3) VM fields/accessors: `DeviceListVM::nameForRow` (the canonical-name field — the SAME string the row text and the detail `Name:` render, so R1 parity is structural) and `DeviceListVM::criticalityForRow`; `ModulesVM::criticalityForRow` (classifies from the snapshot entry the row was formatted from, so marker and Ref/Used-by columns cannot disagree). New `ApplicationFacade::criticalityFacts()` returns the probed facts once — `canDisable()` re-probes the filesystem per call, which a per-row loop must never do; `DeviceListVM` probes in `refreshSnapshot()` (once per model change) and NOT in `rebuild()` (which also runs on every filter keystroke). Row vectors are now reset/appended only through `clearRows()`/`pushNonDeviceRow()` so the five parallel vectors cannot drift out of alignment. Filter haystack extended with the canonical name (users type what they read; the kernel name still matches). No existing shared string changed. **Owner decision 2026-07-23: the Devices marker is IN scope** (Decision 8 had it optional) — facts-first makes it exact. Segmented `StatusLineVM` accessor deliberately NOT built: it is optional here and 9.2/K2 is where composite status lines create the actual need. + 11 unit tests in `tests/unit/test_vm_criticality_accessors.cpp` (name/marker alignment, non-device rows, no-prober degradation, survival across filtering, canonical-name filtering). Full ctest 578/578 green; host format gate clean.

## 8. Pass 2 — bug-fixes B1–B6 (presentation-only; one task per bug incl. its regression test) (T4)

- [x] 8.1 B1 selection invariant `selectedIndex == cursorIndex == detailIndex` + regression test. **Root cause found at apply time:** FTXUI's `MenuBase` tracks TWO indices — `selected()` (what `> `, the VM and the detail pane follow) and `focused_entry()` — and `OnMouseEvent` sets `focused_entry = i` on any mouse event over row `i`, *including a bare move with no button* (menu.cpp:309), while `EntryState::focused = (focused_entry()==i) && menuFocused`. Pass 1 keyed the reverse video off `s.focused`, so one mouse move put the highlight, the `> ` cursor and the detail pane on three different rows. **Fix:** `render::menuRow`/`views::renderDeviceRow` take `(selected, listFocused)` — every selection signal (marker, bold, reverse video) keys off `selected` alone; `listFocused` only says whether the list owns the keyboard, so handing focus to the filter dims the bar without moving the cursor. The four shell transforms pass `s.active` + `menu->Focused()` (each `Component` now declared before its `MenuOption` so the transform can query it). + 4 render tests in `tui/tests/test_selection_render.cpp` (marker and highlight on the same row for every selected index; a non-selected row is never highlighted even with the list focused; selection keeps its row when focus leaves; Devices rows share the invariant at all three sizes).
- [x] 8.2 B2 one accent/inverted-cyan selection treatment across all four views + per-view render test. Pass 1 applied the row's own role decorator and *then* inverted it, so the selection bar was green on an enabled device, red on a disabled one, blue on an available update — a different bar in every view and on every row. `render::menuRow` now gives the selected row `Role::Accent + bold (+ inverted while the list owns the keyboard)` and drops the row's state colour; unselected rows keep theirs. The selected row's state stays legible through its glyph, its own words and the detail pane (§10), and in Mono/Plain the treatment degrades to the same reverse video + `> ` marker everywhere. + 4 render tests (identical style across all 7 role values; identical across the four view row builders; one Mono treatment; unselected rows keep their state colour).
- [x] 8.3 B3 placeholder/header rows non-selectable; empty list → no cursor + empty-state test. New pure `tui/src/selection.{hpp,cpp}` (`nav::snapToSelectable`, `nav::anySelectable`): snaps the cursor off a non-data row, continuing the direction of travel first so arrows read as "skip the header", sweeping back when that direction runs out, and leaving the index alone when NOTHING is selectable (the empty list). Applied once per render pass for the active tab only — the single point every selection-moving path funnels through (arrow keys, a mouse click straight onto a header, a rebuild that re-laid the rows), mirroring FTXUI's own `Menu::Clamp()` render-time fix-up; the snap is pure and idempotent. Selectability comes from the ViewModels, so the frontend re-derives nothing: Devices `isHeader()`, and `signedForRow`/`stateForRow`/`healthForRow` returning nullopt for the "(no …)" placeholders. The four row transforms pass `s.active && <row is data>`, so a placeholder or header can never render the marker/highlight. + 7 tests (5 snap-logic + 2 render: empty list shows no cursor at all three sizes, group header never takes the cursor).
- [x] 8.4 B4 single empty indicator on Snapshots + test. The empty store said "no snapshots" twice — once as the counts banner, once as the list placeholder. Fixed at the source rather than per-surface: `SnapshotsVM::banner()` returns `""` for an empty store (there are no counts to summarise), the TUI snapshots view renders no banner row when the banner is empty, and the GUI gained `MainWindow::updateSnapshotsBannerLabel()` (text + `setVisible(!empty)`, the existing `updateRequestBannerLabel()` idiom) replacing the three raw `setText` sites, with the label starting hidden. The list's `(no snapshots)` placeholder is the single indicator on both surfaces, and the freed row goes back to the 80x24 budget. + 2 render tests (exactly one "no snapshots" on screen at all three sizes; an empty banner shifts the whole body up one row) + updated `SnapshotsVmTest.PlaceholderRowWhenEmptyNeverActionable`.
- [x] 8.5 B5 Modules column order Name, Signed, Ref, Size, Used-by; `(signer)` only when non-empty + 80-col drop-order test. `formatModuleRow` was `%-28s %8lluK %4ld  %-24s %s` (Name, Size, Ref, Used-by, Signed) — with the row clipped from the right inside the 72-column pane, that dropped the signature first and kept Used-by, exactly backwards. Now `%-28s %s %4ld %7lluK  %s` with the signature cell width-normalised to 20 columns: Name(28)+1+Signed(20)+1+Ref(4)+1+Size(8)+2 = 65 ≤ the ~67 usable columns, so Name/Signed/Ref/Size all survive at 80 columns and Used-by is what clips. The cell is padded **by display width, not bytes** (new `displayWidth`/`popCodepoint`/`padTo` helpers) — the async-pending "…" is 3 bytes and 1 column, so printf's `%-Ns` would have jumped every column to its right by two while the fill was in flight. `signatureLabel` emits `yes (signer)` only when the signer is present AND non-empty (it rendered `yes ()` otherwise). Cells now travel in a named `ModuleRowCells` struct so the two strings cannot be swapped (clang-tidy). Row text is shared with the GUI's `ModuleListModel`, so both surfaces reorder together. + 5 unit tests (column order; Signed/Size inside the 80-col clip with an over-long signer ellipsis-truncated; no `yes ()`; pending "…" keeps the columns in place, asserted in columns not bytes).
- [x] 8.6 B6 status line hard-clipped to one row with right-elide + >120-char-at-80-cols test. New pure `render::elidedText` node (`tui/src/render_util.cpp`, first .cpp in the render lib): fills whatever box the layout gives it, keeps the head and spends the last cells on a right ellipsis ("…", `"..."` in Plain so no non-ASCII byte is emitted), flattens `\n`/`\r`/`\t` to spaces, and asks for `min_x = 1` / `min_y = 1` so a long message cannot inflate its parent's layout requirement. `renderStatusBar` uses it plus an explicit `size(HEIGHT, EQUAL, 1)`, making the one-row contract structural rather than a property of the message. **Measured before/after:** the one-row part already held (verified against the pre-fix build); what was actually broken was that a >120-char refusal at 80 columns was cut mid-word with nothing marking the cut — the two elide tests fail against the pre-fix `ftxui::text` and pass after. + 3 render tests (>120 chars at all three sizes: one row, within width, head kept, tail gone, ellipsis present; embedded newline stays on one row; Plain elides with ASCII dots and emits no byte ≥ 0x80) + 1 guard that a short message still fills the row edge-to-edge. Full ctest 603/603.

## 9. Pass 2 — completions (T5)

- [x] 9.1 K1 per-view colour on Updates/Snapshots from existing accessors + confirm Devices non-green states render + FULL/MONO matrix tests. **What was actually missing:** the wiring existed but the state→role switches lived in `runTuiApp`'s anonymous namespace, so nothing could reach them — the only evidence that a disabled device reads danger was a screenshot, and the dev box has no disabled/error device or corrupt snapshot to photograph. Extracted verbatim to `tui/src/state_roles.hpp` (pure, `constexpr`, no toolkit dependency), which required splitting the `Role`/`Glyph` enums out of `theme.hpp`/`render_util.hpp` into a dependency-free `tui/src/semantics.hpp` so the mapping compiles against the app/core enums without pulling FTXUI in. `devmgr_tests` gained `${CMAKE_SOURCE_DIR}` on its include path and now covers the whole design.md "Per-view coloring" table: 9 tests in `tests/unit/test_tui_state_roles.cpp` (every DeviceStatus, ModuleSignature, UpdateRowState, SnapshotHealth incl. health-outranks-HEAD-accent, StatusSeverity, banner escalation, nullopt on non-data rows, and that the two danger device states stay distinguishable by glyph). The other half of the chain — each role actually painting in FULL and no hue in MONO with the state word intact — is 4 new render tests in `tui/tests/test_states_matrix.cpp`, one per view, incl. Devices' four non-green states. `runTuiApp` shrank by ~100 lines. Full ctest 616/616.
- [x] 9.2 K2 status line coloured by `StatusLineVM::severity()` (max severity for composite lines) + tests. **The composite line the task anticipated turned out to be a live defect, not a hypothetical:** the Updates row took `installProgressText()` when non-empty and `statusVm.text()` otherwise, but `UpdatesVM::onCompleted` clears the progress text only for task ids with the install prefix — so a guard refusal raised during an install (`taskId = "guard"`, `ok = false`, e.g. pressing `u` on a status-only row) was **dropped from the screen entirely**, colour and all. Fixed with `composeStatus(...)` in `state_roles.hpp`: segments in display order, empty ones dropped, joined with " · ", and the row takes `maxSeverity` of the segments actually shown (rank Ok < Info < Success < Warning < Danger — only the loud end is load-bearing; Success outranks Info because it reports what the user just did). The shell's four parallel `statusLine()`/`statusRole()` lambdas collapsed into two `StatusRow`-returning functions, so a row's text and its valence are decided in one place and cannot drift about which source they describe. + 5 unit tests (severity ordering incl. Info<Success; empty row is neutral; single segment keeps its valence; progress+refusal share the row at Danger with both texts; absent segments drop without a stray separator). Full ctest 621/621.

## 10. Pass 2 — new-scope presentation (T6)

- [x] 10.1 R2 active-tab highlight (accent+bold; MONO/PLAIN bold + ASCII marker; never yellow). The active tab is now `theme.decorate(Role::Accent) | bold`, and its bracket pair changes `[n]` → `{n}` so the current view survives with no colour at all — bold alone sat directly above the bold legend line and read as the same emphasis. Braces are width-safe ASCII, so the bar's geometry is byte-identical whichever tab is active, and `{` is deliberately none of the other markers in play (focus `>`, criticality `#`, snapshot `*`). `theme` is no longer `[[maybe_unused]]` here. + 4 render tests (accent present and **no yellow pixel anywhere** for each of the four tabs; exactly one `{` and three `[` per row in both Mono and Plain with every byte < 0x80; row width unchanged between tabs; the three markers pairwise distinct).
- [x] 10.2 R3 bounded reveal: extract to `render_util` as a pure function of an explicit offset; reuse in Modules/Updates/Snapshots; non-selected rows keep `ElideRight`; the live time source is gated to "overflowing row selected/focused" and disabled at rest (design Decision 9); audit + convert pass-1's Devices driver if perpetual. **Audit verdict: pass-1's driver WAS perpetual** — `marqueeWindow` computed `tick % cycle`, i.e. pause, scroll, pause, *restart forever*, which is the idle decoration §4.5 forbids and needs a redraw loop that never stops. Replaced by three pure functions in `render_util`: `revealMaxOffset` (glyphs of overflow), `revealWindow(s, width, offset)` (clamped, glyph-based so a multi-byte name never splits), `revealOffset(tick, maxOffset)` (holds through a ~0.6 s lead-in, one glyph per tick, then **rests at max forever** — monotonic and terminating, which is exactly what lets the ticker stop). The shell keeps the single live time source: `revealTick` resets whenever `(tab, selectedRow)` changes (read *after* the B3 snap so a header nudge is not a move), and the ticker posts only while `revealRunning` — set by the transform when `offset < maxOffset`. Now used by **all four** lists (`kWideRowWidth` for the three wide panes), not just Devices. Non-selected rows are not windowed at all: `render::menuRow` renders through `elidedText`, so they elide right visibly. + 6 tests (short name never reveals; head/tail at the two resting offsets and no wrap-around; monotonic-and-terminating over 100 ticks; no mid-codepoint split at any offset for a name with "™"/"—"; row within region and nothing written to a second row at offsets 0/mid/max at all three sizes; non-selected rows elide with a visible "…" and no tail).
- [x] 10.3 R4 criticality marker (Modules required; Devices optional) — marker on name only; never recolours +/- or signed glyph; never danger. **Devices included** (owner decision recorded in 7.3: facts-first classification makes the marker exact). New `render::Badge{glyph, role}` in `semantics.hpp` plus `Glyph::Essential` (`#`) and `Glyph::Important` (`~`) — ASCII in *every* mode including the Unicode opt-in, because the Unicode warning signs are ambiguous-width (§8) and would reflow the row. `menuRow` renders a badge as a **separate element between the prefix and the label**, so it colours only itself: the `+/-` state glyph before it and the signature/version cells inside the label after it keep their own semantics. `badgeForCriticality` maps Essential and Important both to **warning, never danger** (design Decision 7) and Ordinary to no badge at all. The badge keeps its warning colour even on the selected row (FTXUI's nested colour decorators render inner-last), because criticality belongs to the component, not to where the cursor is. Row widths shrink by 2 when a badge is present so the reveal still fits. The word lives in `ModulesVM::detailLines()` — a new `Risk: essential — unloading this may make the system unusable` line read from `criticalityForRow`, so marker and words cannot describe different modules. **Layout bug caught by the ASCII assertion:** `elidedText` asks for one column so it can never inflate a layout, which inside the badge hbox meant it was *given* one column and the whole label collapsed to "…"; fixed with `xflex` on the label segment. + 6 render tests (badge column warning while the `+` stays green and the label keeps its state colour; no red pixel anywhere for either level; essential+unsigned coexist in different columns; the two levels are distinct ASCII glyphs in all 3 modes × unicode opt-in; ordinary rows unmarked; badge survives the selection treatment) + 2 VM tests (risk line named for an essential module; absent for an ordinary one). Full ctest 639/639.
- [x] 10.4 R5 muted column headers (Modules + Updates); non-selectable; also in the collapsed 80-col list-only view; 80x24 row budget holds. New `ModulesVM::columnHeader()` / `UpdatesVM::columnHeader()` build the header from the **row formatters' own printf widths**, so header and rows cannot drift apart — that is why the wording lives in the VM and not in the view (§11: the frontend re-derives nothing). The views render it as a plain muted row inside the list pane, **not** as a Menu entry, so it is structurally incapable of taking the cursor; an empty header string renders no row at all. Modules: `Name Signed Ref Size Used-by` under the filter field; Updates: `Src Device Version -> New` plus a rule (that pane has no filter field to separate it from). Note on "collapsed 80-col list-only view": no such collapsed layout exists in the build — the 80–109 col range still renders both panes — so the header renders in the list in every layout; the collapsed-view question is carried by 12.1/K3 where the toggle itself is verified. GUI headers are out of scope (R6 covers only canonical name + criticality text). + 6 Modules render tests (present, muted-dim, above the rows, exactly one, cannot take the cursor, 80x24 budget with two data rows + banner + status, plain text in Mono/Plain, no row when empty) + 3 Updates render tests + 2 VM alignment tests asserting the header columns land on the same byte offsets as a real row.
- [x] 10.5 render tests for 10.1–10.4 (incl. the three markers pairwise distinct). Each of 10.1–10.4 landed with its own render tests (4 + 6 + 6 + 9 above). This task adds the cross-cutting one: `MarkerDistinctness.ActiveTabFocusAndCriticalityAreThreeGlyphs` renders the tab bar, a selected essential row and an ordinary row **together** on one screen in Mono AND Plain, and asserts all three markers are present, are three different glyphs (`{2}`, `> `, `#`), and that none of them leaks onto a row it does not belong to. Full ctest 649/649; devmgr_tui_tests 33 → 83.

## 11. Pass 2 — detail + GUI parity (T7, T8)

- [x] 11.1 R1 canonical `Name:` in detail + `Address:`/`VID:PID:`/`Id:` rows + bounded long values (wrap/scroll) at 80 cols + render tests. `Name:` (canonical, from the same `core::displayDeviceName` the list rows use) already landed in 7.1; this adds the missing `Address:` row via a new `core::displayDeviceAddress(device)` — the kernel's positional address alone (`0000:c5:00.4`, `3-3`), which is what correlates a row with `lspci`/`lsusb`/dmesg output and which would otherwise be **lost** now that the list label is a name rather than that address. The identity block is reordered to `Name → Address → VID:PID → Id`, then Bus/Status/Sysfs. Bounded values: new pure `views::renderDetailPane(lines, theme)` renders every line through `render::elidedText`, and all four tabs' detail panes (plus the snapshots diff body) now go through it — a modalias or sysfs path at 80 columns ends in a visible ellipsis on its own row instead of running to the screen edge and stopping mid-token. + 5 render tests in `tui/tests/test_detail_pane_render.cpp` (identity rows in order at all three sizes; one VM line = exactly one screen row with no wrapped continuation and nothing over width; a >80-col modalias elided with the row below untouched; Plain mode ASCII-only ellipsis; empty list renders nothing) + 1 VM test asserting the four leading rows and that the canonical name — not the positional `0000:c5:00.4` — is the `Name:`.
- [x] 11.2 R6 GUI canonical name + criticality text in device list/detail + GUI offscreen test (colour deferred; runs in the container gate). **Implementation was already structural** (landed with 7.1/7.3/10.3): the GUI device/module lists render `DeviceListVM::rowsRef()` — built from `core::displayDeviceName` — so the canonical name is the primary label on both surfaces from one formatter; `DeviceListModel`/`ModuleListModel::data` append the criticality WORD from the shared `criticalityForRow()` when non-Ordinary (DESIGN §9: colour is TUI-only, so the GUI spells the same fact out); the GUI device detail pane renders `DeviceDetailVM::lines()` verbatim, which already carries the canonical `Name:`, the `Address:` row and a `Risk:` criticality line. **This task adds the missing proof:** `gui/tests/test_r6_parity.cpp` (3 offscreen tests wired into `devmgr_gui_tests`, `QT_QPA_PLATFORM=offscreen`, real `QtUiDispatcher` + `FakeCriticalityProber` — the exact composition-root seam). (1) an uncatalogued device (raw name = kernel address, properties resolve `AMD USB controller`) shows the canonical name as the PRIMARY list label with the raw id demoted to secondary text, and the same string in the detail `Name:` row with the address surviving as its own `Address:` row; (2) an essential device (root-backing sysfs via the prober) carries the criticality word in its list DisplayRole text AND its detail `Risk:` line while the model exposes NO Decoration/Foreground role (proving the fact rides in text, not colour), and the guard-allowed control device carries no word; (3) a curated-essential module carries the word in the module list while an ordinary one does not. Full `devmgr_gui_tests` 73 → 76 green; target builds clean.

## 12. Pass 2 — verification + exit gate (T9, T10)

- [x] 12.1 K3 80-col toggle render test; K4 filter no-leak key-routing test. **Key set = the full command-key union** `{ r, e, U, B, q, l, u, d, s, h, x, m, / }` plus nav `{ Tab, ArrowUp, ArrowDown, Esc, Enter }`; assert each *types into* the active filter and does not invoke its command. Include both `U` (unbind) and `u` (unload/install) because the filter is case-insensitive (this case mismatch is the leak surface); drop any stray lowercase `b` (bind is uppercase `B`). Digits `0–9` SHALL type into the filter (the legend binds no digit handler; if code later adds one, re-evaluate). `/` while filtering SHALL not re-trigger/loop; `Esc` SHALL clear and restore list focus. Implementation lever: the active filter is a focused `Input` whose `CatchEvent` consumes `Event::Character` before the list handler sees it. **Both toggles extracted to pure, off-screen-testable seams** (this change's discipline: a conditional the shell owns becomes a pure function the shell delegates to). **K3:** the only width-driven layout switch in the build is the DESIGN §3.2 minimum-size guard — there is no list-only collapse (10.4 established this), so the "80-col toggle" IS the 80x24 guard. `views::belowMinimumSize(cols, rows)` + `views::renderMinSizeNotice()` (`tui/src/views/min_size.{hpp,cpp}`, pure, no colour) replace the inline shell guard and the local `kMinCols`/`kMinRows`; `tui/tests/test_min_size_render.cpp` (3 tests) proves the boundary (≥80x24 → full UI, short in *either* dimension → notice), that the notice names the minimum + both exits and fits inside a below-min screen with no row over width, and ASCII-only. **K4:** `nav::routeFilterKey(bool filterFocused, const ftxui::Event&) -> {NotFiltering, PassToInput, HandBackToList, ClearAndHandBack}` (`tui/src/key_routing.hpp`, header-only over the real `ftxui::Event` — the actual leak surface, incl. case twins) replaces the shell's inline `if (Return)/if (Escape)/return false` block; while filtering, EVERY key except Return/Escape → `PassToInput`, so nothing reaches a command. `tui/tests/test_filter_routing.cpp` (6 tests, in its own `devmgr_tui_input_tests` exe because `Event` needs `ftxui::component`, which the pure render lib omits) drives the whole command-key union + digits `0–9` → `PassToInput`; `/` literal while filtering but opens it when not; Return → `HandBackToList`, Escape → `ClearAndHandBack`; nav keys → `PassToInput`; and the complement — not filtering, the same keys report `NotFiltering` so single-key verbs stay live. Full ctest **667/667** green; TUI self-test intact (shell wiring unchanged); host format gate clean (container parity folded into 12.2).
- [x] 12.2 Gates over touched `core/`+`app/`+`gui/`+`tui/` (GUI in the container gate); container ctest green. **All green 2026-07-23** (image rebuilt — no volume mount, so pass-2 sources are only in the gate after a fresh `build unit`): container `ctest` **668/668** (GUI offscreen suite incl. the new R6 parity tests runs here under `QT_QPA_PLATFORM=offscreen`); container **clang-format-18** exit 0 over the full `core tests app platform tui gui daemon cli` set (v18 agrees with the host v22 on every new file); Qt purity (no Qt in core/app/platform/daemon/cli) and sdbus purity guards **PASS**; container **clang-tidy-18** `--warnings-as-errors='*'` over `core/src app/src platform/linux/src gui/src daemon/src cli/src tui/src tui/src/views` exit 0. **The gate earned its keep:** it caught the ONE pass-2 tidy violation the per-group host builds missed — `tui/src/views/tab_bar.cpp`'s R2 marker lambda (group 10.1) took two adjacent `const char*` (`bugprone-easily-swappable-parameters`); fixed by deriving the direct-access digit from the tab index (`std::to_string(tab + 1)`) instead of passing it, which drops one `const char*` param AND removes the redundant argument, output byte-identical (all 6 tab-bar/marker render tests still green).
- [ ] 12.3 Extended manual matrix in a **real terminal** at 120x32 / 100x28 / 80x24 × Full / Mono / Plain. Re-run the original 6.3 matrix AND re-confirm B1–B6 visibly: selection bar == `>` cursor == detail row; selection colour identical across all four tabs; status line is one row at 80 cols with the long Updates line; empty Snapshots has no cursor on the placeholder. Plus the §E additions: active tab distinguishable without colour; long names reveal on the selected row in every tab; Signed is the 2nd column; Updates rows coloured by state; no row/line wraps or overflows; edges — empty Snapshots single indicator, long status at 80 cols one row, essential-module marker + word in Mono/Plain; plus K3/K4.
- [ ] 12.4 `openspec validate tui-instrument-panel-polish --strict` green; DESIGN §13 report (shared states + both surfaces considered, incl. canonical-name/criticality parity; executable checks run; remaining manual checks + why not automatable: real terminal/keyboard, reveal timing, real daemon-down); user commits.
